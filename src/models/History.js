export default class History {
  constructor({
    start,
    last,
    interval,
    data,
  }) {
    this.start = start;
    this.last = last;
    this.interval = interval;
    this.data = data;
  }
}
